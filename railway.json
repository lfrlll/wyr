{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "sh -lc 'export DATABASE_URL=\"${DATABASE_URL:-$DATABASE_PUBLIC_URL}\"; npx prisma db push && npm run start'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
